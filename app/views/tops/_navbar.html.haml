%nav.navbar-default
  .container-fluid
    .navbar-header
      %button.navbar-toggle.collapsed{"aria-expanded" => "false", "data-target" => "#bs-example-navbar-collapse-1", "data-toggle" => "collapse", :type => "button"}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      = link_to "Park Connect", root_path, class: "navbar-brand"
      #bs-example-navbar-collapse-1.collapse.navbar-collapse
        %ul.nav.navbar-nav.navbar-right
          - if user_signed_in?
            %li.navbar-name
              = current_user.name
              = link_to edit_user_path(current_user) do
                = icon('fas', 'cog', class: 'icon')
            %li.navbar-login
              = link_to "ログアウト", destroy_user_session_path	, method: :delete
          - else
            = link_to "ゲストログイン（閲覧用）", home_guest_sign_in_path, method: :post, class:"navbar-btn"
            = link_to "新規登録", new_user_registration_path, class:"navbar-btn"
            = link_to "ログイン", user_session_path, class:"navbar-btn"